import React from 'react';
import createPureComponent from 'UTIL/createPureComponent';
import ReviewContainer from './blocks/ReviewContainer';

export default createPureComponent(({ }) => {
    return (
        <div>
            <ReviewContainer />
        </div>
    );
});
