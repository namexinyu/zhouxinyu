import React from 'react';
import createPureComponent from 'UTIL/createPureComponent';
import ReviewEdit from './blocks/ReviewEdit';

export default createPureComponent(({ }) => {
    return (
        <div>
            <ReviewEdit />
        </div>
    );
});
